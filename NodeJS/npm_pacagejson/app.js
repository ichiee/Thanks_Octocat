var moment = require('moment');
console.log(moment().format("ddd, hA"));

/*
this is how you use package from npm
use require function to get 'moment, 
it will look for the file or directory from Node core first then look for place like node_module

moment is a package for time and date, see documentations on npm
*/
