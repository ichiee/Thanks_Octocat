var greet = require('./greet.js');

// ./ is this dictionary
// require is recived what module exported on the module file
// so greet function from module is assigned to var greet on app.js
// you can also omit .js using just greet instead. still works

greet();
