

### Setting up new branch

1. first make a branch on github - let's say stage1 - by typing a name on branch
2. Go to VS code, go bottom left where it says master to stage1
3. go to top of VS code create a branch - this does not corresponding to your remote github stage 1 yet
4. alt + ~ to open a console on VS code (type something to connect - forgot ***)

5. once done the satge you can marge it back to master by (**** check what coomand wsas used on VS code)




### set up email & name
https://help.github.com/articles/setting-your-commit-email-address-in-git/


    git config --global user.email "email@example.com"
    git config --global user.name "Mona Lisa"

    git config --global user.email                        (to see return value)
        //email@example.com
    
    git config --global user.name
        //Mona Lisa
    
### use shortcut

    if you type first three letters then it will give you an option
